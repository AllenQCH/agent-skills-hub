interface:
  default_prompt: Use $alidocs-browser-reader. Read the provided AliDocs URL through the local Chrome DingTalk session and return title plus visible article text.
  display_name: AliDocs Browser Reader
  short_description: Read AliDocs via Chrome session.
