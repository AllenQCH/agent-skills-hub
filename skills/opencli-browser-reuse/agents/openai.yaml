display_name: OpenCLI Browser Reuse
short_description: Reuse stable opencli browser sessions and avoid tab sprawl.
default_prompt: Use $opencli-browser-reuse before exploring a webpage with opencli so the browser session is reused by domain, one-shot reads use close-after, and new request-specific session names are avoided.
