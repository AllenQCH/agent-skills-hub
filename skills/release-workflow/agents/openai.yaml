interface:
  default_prompt: Use $release-workflow. Prepare a test-release document or locate the relevant CI/CD pipeline.
  display_name: Release Workflow
  short_description: Generate test docs and open pipelines.
